# Candidate Retrieval

Multi-term search should first retrieve candidate document sets for each term. Intersection can model AND semantics; union can model OR semantics when that behavior is introduced explicitly.
