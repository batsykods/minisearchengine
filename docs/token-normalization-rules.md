# Token Normalization Rules

Indexing and querying must apply the same normalization rules. This prevents equivalent terms with different casing or token representation from producing inconsistent lookup behavior.
