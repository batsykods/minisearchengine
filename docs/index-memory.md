# Index Memory Considerations

Posting lists should avoid unnecessary copies during lookup. Const references and standard containers keep the current interface simple while leaving room for compact posting-list storage later.
