# Performance Baseline

The first benchmark baseline should measure index construction and representative query latency independently.

Baseline runs must use the same compiler mode, corpus, and query set when comparing later changes.

Performance regressions should be evaluated with percentile latency rather than a single average.
