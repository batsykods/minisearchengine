# Index Lookup

Term lookup should remain independent from query parsing. The inverted index stores the relationship between normalized terms and document identifiers, allowing direct retrieval without scanning every document.
