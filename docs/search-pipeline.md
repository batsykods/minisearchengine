# Search Pipeline

Search processing is separated into query parsing, term normalization, index lookup, and result collection. Keeping these stages isolated makes later ranking work easier to integrate.
