# Benchmark Corpus

Benchmark runs should use a fixed corpus snapshot so results are comparable.

The benchmark record should capture the corpus revision, document count, vocabulary size, compiler configuration, and machine environment.

Changing the corpus invalidates direct latency comparisons.
